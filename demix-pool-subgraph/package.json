{
  "name": "demix-pool-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --access-token $TOKEN",
    "deploy:mainnet": "yarn deploy -- DecentraWorldDEWO/DecentraMix_Contracts/demix-pool-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.22.3",
    "@graphprotocol/graph-ts": "0.22.1"
  }
}
